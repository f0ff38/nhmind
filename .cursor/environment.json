{
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },
  "install": "npm ci --prefix modules/hello && npm ci --prefix modules/module-template",
  "start": "command -v docker >/dev/null && (sudo service docker start 2>/dev/null || true) || true"
}
