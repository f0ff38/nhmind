# nostr-rs-relay — nhmind coordinator bus (Phase 2 canary).
# Docs: https://github.com/scsibug/nostr-rs-relay

[info]
name = "nhmind-relay"
description = "NostrHiveMind Phase 2 canary relay"

[network]
address = "0.0.0.0"
port = 8080

[database]
data_directory = "/usr/src/app/db"

[limits]
messages_per_sec = 20
subscriptions_per_min = 240
